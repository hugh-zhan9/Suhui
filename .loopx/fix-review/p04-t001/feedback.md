# Child 04 T-001 Review Feedback Ledger

| ID     | Severity  | Source                         | Finding                                                                                                                    | Basis                                                                            | Decision       | Evidence                                                                                                                                                                                                                                                               | Verification                                                                                                                                                                   | Re-review                                                | Status |
| ------ | --------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | ------ |
| FR-001 | Important | canonical task review F-001    | legacy unread source normalization uses `COALESCE` instead of subscription type and can misattribute list/inbox counts     | AC-7; D-008; prior type-directed behavior; unconstrained nullable source columns | accepted_fixed | `service.ts` uses a type-keyed SQL `CASE`; `service.test.ts` asserts feed/list/inbox mapping with overlapping nullable columns, legacy-only counts, and derived-over-legacy precedence                                                                                 | focused unread/manager Vitest 52/52 PASS; main typecheck unchanged baseline-blocked with no scoped diagnostics; Prettier, diff, staged-state, and migration/schema guards PASS | attempt-3 canonical re-review: SPEC_COMPLIANT / Approved | closed |
| FR-002 | Important | canonical task re-review F-001 | parameterized type-keyed `CASE` compiles to different placeholders in SELECT and GROUP BY and can fail PostgreSQL grouping | AC-5; AC-7; D-008; generated full SQL                                            | accepted_fixed | `service.ts` emits fixed `feed`/`list` discriminator literals; `service.test.ts` executes the service through Drizzle/node-postgres and proves the complete statement repeats one structurally identical `CASE` in SELECT/WHERE/GROUP BY with an empty parameter array | focused unread/manager Vitest 53/53 PASS; main typecheck unchanged baseline-blocked with no scoped diagnostics; Prettier, diff, staged-state, and migration/schema guards PASS | attempt-3 canonical re-review: SPEC_COMPLIANT / Approved | closed |

## Feedback Lancet Decision

- FR-001: replace only the legacy source-ID expression with a SQL `CASE` keyed by the existing subscription type; extend the existing focused query test for feed/list/inbox rows with overlapping non-type columns and derived-over-legacy precedence. No schema constraint, migration, fallback, helper layer, or new dependency.
- FR-002: keep the type-keyed mapping but remove repeated independently bound placeholders, using the smallest PostgreSQL-valid representation (closed enum literals or grouping by the selected expression/ordinal). Add a full generated-statement assertion; no schema or query-layer abstraction.

## Verification Evidence

- `pnpm --filter @suhui/electron-main exec vitest run src/application/unread/service.test.ts src/remote/manager.test.ts`: PASS, 2 files and 53 tests (the requested full-statement regression increases the prior 52-test suite by one).
- `pnpm --filter @suhui/electron-main typecheck`: unchanged baseline-blocked by existing `rootDir`/file-list and unrelated test/plugin/database diagnostics; no diagnostic references the unread service or test.
- Targeted Prettier, `git diff --check`, empty staged-state check, and migration/schema guard: PASS.
- Re-review remains required before this Important finding can close.
